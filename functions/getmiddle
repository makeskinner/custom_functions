function getmiddle(text, leftSeparator, rightSeparator) {
    let parts = text.split(leftSeparator);
    if (parts.length < 2) return ""; // If left separator is not found, return empty
    let rightParts = parts[1].split(rightSeparator);
    return rightParts[0]; // Return the part between the separators
}